# 공식 현재 관측 연결 — 2026-09-19

[공개 지도](https://korea-replay.legacy.example.invalid/)의 **현재 관측**에서 버스 위치, 서울 1호선 역 상태, 레이더·위성 원본 영상을 조회할 수 있습니다. 현재 공개 버전은 `ab42e105-8e29-4078-806b-cb20fb799662`입니다. 이 문서는 실제 공개 결과와 아직 로컬에서 검증 중인 개발을 구분합니다.

## 공개 결과

2026-09-19 22:30 KST의 최종 공개 조회 결과입니다. 수량은 조회 시점에 따라 바뀝니다.

| 자료 | 실제 응답 | 정확한 의미 |
|---|---|---|
| 서울 1호선 | HTTP 200, 원천 73행에서 중복 8건·오래된 4건을 제외한 61건, `partial` | 역 이름·방향·상태·원천 수신 시각. 차량 GPS와 원천 측정 시각은 제공되지 않아 `position:null`, `observed_at:null` 유지 |
| 위성 | HTTP 200, `available`, 원본 13패널 | 천리안2A 적외영상. 원본 지도·범례를 포함한 패널이며 정합된 지도 오버레이 아님 |
| 레이더 | HTTP 200, `available`, 원본 13패널 | 공식 원본 패널. 배경색만으로 무강우 또는 정밀 강우량을 판정하지 않음 |
| 부산 1001 | HTTP 200, `available`, 19대 | 선택 노선의 최신 위치 응답. 연속 운행 이력이나 전국 동시 관측 아님 |

별도의 전국 버스 표본에서는 **17개 도시·17개 노선**을 한 번씩 조회했습니다. 16개 노선에서 차량 합계 160대가 응답했고, 울릉은 정상 빈 응답이었습니다. 전국 모든 노선의 정확성 검증으로 확대 해석하지 않습니다. 근거는 `.local/national-live-20260919/report.json`입니다.

실제 공개 UI에서는 대한민국 전체 화면에서 서울 1호선 목록과 위성 영상을 확인했습니다. 당시 역 상태는 65건, 위성 관측 시각은 22:24 KST였습니다. 이미지의 실제 디코딩 크기는 600×614이고 콘솔 오류는 없었습니다. 앞선 원본 PNG 검사는 463,938 B, 60개 청크 CRC와 전체 픽셀 디코딩을 통과했습니다. 서로 다른 조회 시점의 수량을 합산하지 않습니다.

## 인증과 전송 경계

서울 도시철도 인증키는 공식 신청 후 발급·정상 상태를 확인하고 서버의 비밀 설정으로 등록했습니다. 기상청 위성영상 조회서비스는 사용자가 최종 신청을 완료한 뒤 포털의 승인 상태와 실제 원천 응답을 확인했습니다. 키는 프런트엔드·소스·문서·공개 증빙에 넣지 않습니다.

서울 원천의 HTTPS 요청은 로컬과 Cloudflare 미리보기에서 제한 시간 안에 응답하지 않았습니다. 따라서 서버 설정 `SEOUL_SUBWAY_ALLOW_HTTP=true`를 명시적으로 사용했습니다. **사용자 브라우저와 Cloudflare 사이는 HTTPS이고, Cloudflare에서 서울 원천으로 가는 구간은 공식 문서에 기재된 HTTP입니다.** TLS 검증을 끄거나 실패 시 임의 호스트로 우회하는 방식이 아닙니다. [서울시 공식 실시간 열차 위치 API](https://data.seoul.go.kr/dataList/datasetView.do?infId=OA-12601&serviceKind=1&srvType=A)

현재 허용된 서울 노선은 **1호선 하나**입니다. 기본 안내 한도 1,000회/일을 기준으로 조회 간격과 예산을 제한합니다. 현재 공개판의 사용량 가드는 isolate·지역 캐시 범위이며 `global_enforced:false`입니다. 여러 지역의 서버를 묶는 중앙 가드를 공개한 상태는 아닙니다.

## 배포와 복구 검증

기존 공개 `3b992618-b351-47b9-96bc-21c37e617413`을 바탕으로 비밀 설정만 새 버전에 등록했습니다. 웹·Worker 코드와 정적 자료는 묶음 **`579c2245026b946b`**, 데이터 **`pub-a55c6a081e5782db`**, **17,401파일·10,725,167,860 B**로 같습니다. 이번에는 10GB 파일 전체 해시를 반복하지 않았으며, 이전 묶음 감사와 새 원격 검사를 구분합니다.

- `http-preview-release.json`: 새 버전 미리보기 **67/67** 통과.
- `public-release.json`: 전환 직후 이전 Worker가 응답해 **66/67**. 실패 기록도 보존.
- `public-release-settled.json`: 배포 전파 후 공개 **67/67** 통과.
- `rollback-roundtrip.json`: 실제 공개 트래픽 **ab42 → 3b → ab42** 전환, 버전과 서울 비밀 설정의 복구 확인. 롤백 중 전체 67개 검사를 다시 수행한 것은 아님.
- `public-live-final.json`: 복귀 후 runtime·지하철·위성·레이더·부산 버스 5개 응답과 비밀값 유출 검사 통과.
- `public-ui.json`, `public-ui.png`: 실제 공개 화면 검수. 성능 측정용 화면이 아님.

위 파일은 모두 `.local/seoul-live-20260919/`에 있습니다. 이전 3b 공개판에서 013966a1로 되돌렸다가 복귀하며 각각 67개를 검사한 증빙은 `.local/map-first-20260919/`에 따로 보존합니다.

다음 코드 배포는 기존 비밀 설정에 서울 인증키와 HTTP 정책을 함께 보존해야 합니다. 로컬 비공개 통합 입력은 `.local/auth/secrets-live-20260919.json`입니다. 과거 비밀 입력만 다시 업로드하면 새 연결이 누락될 수 있습니다. 이 파일은 코드·소스 ZIP·정적 자산에 포함하지 않습니다.

## 로컬 구현과 남은 작업

최종 로컬 웹 **541개·38파일**, Python **394개**, 타입·린트·프로덕션 빌드가 통과했습니다. Python의 기존 rasterio/affine 폐기 예고 경고 4개와 Cesium 번들 크기 경고는 남아 있습니다. 브로커의 Wrangler `deploy --dry-run`도 통과했으며, 이 명령은 원격 업로드·DO 생성을 수행하지 않았습니다. 실행 로그는 같은 증빙 폴더의 `web-tests.log`, `python-tests.log`, `lint.log`, `build.log`, `broker-dry-run.log`입니다.

중앙 호출 예산용 비공개 Worker·SQLite Durable Object, 원천 파서 재사용 경로와 회귀 검사는 로컬에서 구현했습니다. **중앙 브로커는 아직 Cloudflare에 배포하거나 지도 Worker에 연결하지 않았습니다.** 무료 한도의 실제 쓰기·요청량, Service binding, 최초 생성과 독립 롤백을 검증한 뒤 연결합니다. [중앙 호출 예산 설계·구현 상태](LIVE_BROKER_PLAN.md)

전국 건물 부품 후보는 원천 형제 개수와 높이 충돌을 검사하여 **16개 부모·61개 부품**까지 선정했습니다. 사설 GLB 16개의 형식 검사 오류·경고는 0입니다. 공개 타일의 교체·중복 제거·실제 지붕 정확성 검증은 아직 수행하지 않았습니다. [건물 원천 업그레이드](NATIONAL_DATA_UPGRADE.md)

연속 교통 수집·보관, 모든 서울 노선, 전국 실제 건물 정확성, 전경 브라우저 P95 33ms·모든 생성 작업 8ms는 미완료입니다. 자동화 브라우저에서 관측된 약 1초 프레임 간격은 활성 창·렌더링 제한의 영향을 분리하지 못해 성능 합격 판정에서 제외했습니다. `.local/foreground-20260919/`에 진단만 보존했습니다. 로컬 삭제는 외부 백업·복원과 남은 개발 조건이 충족될 때까지 실행하지 않습니다.

> 공개 소스에서는 과거 개인 계정의 배포 호스트를 `legacy.example.invalid`로 대체했습니다. 버전·측정 이력은 당시 기록이며 현재 서비스 상태와 구분합니다. 실제 과거 URL은 비공개 배포 증빙에 보존합니다.
